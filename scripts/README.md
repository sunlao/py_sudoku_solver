# Support Scripts

Miscelaneous scripts that do not belong in source code that can be used durring development

repo - count files in repo: `python -m scripts.repo src`
reset - reset repo by deleting things created during develop: `python -m scripts.reset`
