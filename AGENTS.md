# py_sudoku_solver

## Project context

Before analyzing the architecture or proposing changes:

1. Read all specification files in `docs/llm_spec/`.
2. Inspect all architecture diagrams in `docs/`.
3. Read SVG files as source and render them when their visual layout or relationships matter.

Treat the specifications and diagrams as authoritative project context
